import React from "react";
import classes from "./Favorites.module.css"
const Favorites = () => {
  return <div className={classes.favorites}>We are updating this feature.Please try again later!</div>;
};

export default Favorites;
